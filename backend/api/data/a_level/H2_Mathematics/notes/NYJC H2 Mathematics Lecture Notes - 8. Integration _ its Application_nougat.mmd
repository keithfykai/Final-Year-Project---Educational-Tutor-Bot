## Chapter 11 Techniques of Integration

### 11.1 The Indefinite Integral

#### Introduction and Definitions

The process of **integration** is the reverse of differentiation. For example, when we differentiate \(x^{2}\), \(x^{2}+1\) or \(x^{2}-3\) and so on, we obtain \(2x\). When we integrate \(2x\), the answer would be \(x^{2}+C\) where \(C\) is an **arbitrary constant**.

Consider two functions \(\mathrm{f}(x)\) and \(\mathrm{F}(x)\) which are related as follows:-

\(\frac{\mathrm{d}}{\mathrm{d}x}\big{(}\mathrm{F}\big{(}x\big{)}\big{)}\)= \(\mathrm{f}\big{(}x\big{)}\).

\(\mathrm{f}(x)\) is called the **derivative** of \(\mathrm{F}(x)\) with respect to \(x\) and \(\mathrm{F}(x)\) is called an **anti-derivative** of \(\mathrm{f}(x)\) with respect to \(x\).

If \(C\) is a constant, then we have

\(\frac{\mathrm{d}}{\mathrm{d}x}\big{[}\mathrm{F}\big{(}x\big{)}+C\big{]}\)= \(\frac{\mathrm{d}}{\mathrm{d}x}\big{[}\mathrm{F}\big{(}x\big{)}\big{]}\)+ \(\frac{\mathrm{d}}{\mathrm{d}x}\big{[}C\big{]}=\mathrm{f}(x)+0=\mathrm{f}(x)\).

Hence by our definition, the family of functions \(\mathrm{F}(x)+C\) where \(C\) is an arbitrary constant are all anti-derivatives of \(\mathrm{f}(x)\) if \(\mathrm{F}(x)\) is an anti-derivative of \(\mathrm{f}(x)\).

We denote all anti-derivatives of \(\mathrm{f}(x)\), \(\mathrm{F}(x)+C\), by the notation \({}^{*}\)\(\mathrm{f}(x)\)\(\mathrm{d}x\)\({}^{*}\) which reads as 'the **indefinite integral of \(\mathrm{f}(x)\) with respect to \(x\).' That is, \(\int\mathrm{f}(x)\,\mathrm{d}x=\mathrm{F}(x)+C\).

**Remarks:**

1. The function \(\mathrm{f}(x)\) within the integral sign \({}^{*}\)\(\big{[}\)\({}^{*}\) is called the **integrand**.
2. The process of finding \(\int\mathrm{f}(x).\mathrm{d}x\) is called **integration**.
3. The arbitrary constant \(C\) arising from integration is called the **constant of integration**.

#### Properties of the Indefinite Integral

1. \(\int 0\)\(\mathrm{d}x=C\) where \(C\) is an arbitrary constant.
2. \(\int\left[\mathrm{f}(x)\pm\mathrm{g}(x)\right]\,\mathrm{d}x=\int\mathrm{f}(x )\,\mathrm{d}x\pm\int\mathrm{g}(x)\,\mathrm{d}x\).
3. \(\int k\mathrm{f}(x)\,\mathrm{d}x=k\int\mathrm{f}(x)\,\mathrm{d}x\) for any real constant \(k\).

[MISSING_PAGE_EMPTY:2]

[MISSING_PAGE_EMPTY:3]

**Self-Review 11.1**

Find the following integrals:

(a) \(\int\!\!\sqrt{4x-3}\;\mathrm{d}x\;\left[\frac{1}{6}\big{(}4x-3\big{)}^{\frac{3}{2} }+C\right]\) (b) \(\int\!\!\left(4x+\frac{1}{\sqrt{x}}\right)^{2}\mathrm{d}x\;\left[\frac{16}{3}\,x ^{3}+\frac{16}{3}\,x^{3}+\ln\!\left|x\right|+C\right]\) (c) \(\int\!\!\frac{x}{x^{2}+1}\mathrm{d}x\;\left[\frac{1}{2}\ln\!\left(x^{2}+1 \right)+C\right]\) (d) \(\int\!\!\frac{2x}{\sqrt{1-4x^{2}}}\mathrm{d}x\;\left[-\frac{1}{2}\sqrt{1-4x^{ 2}}+C\right]\) (e) \(\int\!\!\sec^{2}x\tan^{3}x\;\mathrm{d}x\left[\frac{1}{4}\tan^{4}x+C\right]\) (f) \(\times\!\!\sec^{3x}\!\mathrm{d}x\;\left[\frac{1}{6}\,\mathrm{e}^{3x^{2}}+C\right]\) (g) \(\int\!\!\left(9x^{2}+1\right)\tan^{-1}3x\;\mathrm{d}x\;\left[\frac{1}{3}\ln \!\left|\tan^{-1}3x\right|+C\right]\)

[MISSING_PAGE_FAIL:5]

[MISSING_PAGE_FAIL:6]

[MISSING_PAGE_EMPTY:7]

[MISSING_PAGE_FAIL:8]

[MISSING_PAGE_EMPTY:9]

(e) \(\int\frac{1}{2x^{2}+4x-5}\,\mathrm{d}x\) (Self-Reading)

**Solution:**

(a)

Factor out 9 so that the coefficient of \(\mathbf{x}^{2}\) is \(\pm 1\).

Alternatively, we rewrite the integral in the general form and use the result

\(\int\frac{\mathrm{f}\,\dot{\gamma}(x)}{\sqrt{a^{2}-\left(\mathrm{f}(x)\right)^ {2}}}\,\mathrm{d}x=\sin^{-1}\left(\frac{\mathrm{f}(x)}{a}\right)+C\) as below:

\(\int\frac{1}{\sqrt{1-9x^{2}}}\,\mathrm{d}x\) = \(\int\frac{1}{\sqrt{1-(3x)^{2}}}\,\mathrm{d}x\) = \(\frac{1}{3}\int\frac{3}{\sqrt{1-(3x)^{2}}}\,\mathrm{d}x\) = \(\frac{1}{3}\sin^{-1}3x+C\)

Factor out 16 so that the coefficient of \(\mathbf{x}^{2}\) is \(\pm 1\).

Alternatively we use the general result:

\(\int\frac{\mathrm{f}\,\dot{\gamma}(x)}{a^{2}+\left(\mathrm{f}(x)\right)^{2}}\, \mathrm{d}x=\frac{1}{a}\tan^{-1}\left(\frac{\mathrm{f}(x)}{a}\right)+C\)

\(\int\frac{7}{5+16x^{2}}\mathrm{d}x\) = \(\int\frac{1}{\left(\sqrt{5}\right)^{2}+\left(4x\right)^{2}}\, \mathrm{d}x\)

\(\int\frac{4}{4}\left(\sqrt{5}\right)^{2}+\left(4x\right)^{2}\, \mathrm{d}x\)

\(\int\frac{7}{4}\frac{1}{\sqrt{5}}\,\tan^{-1}\left(\frac{4x}{\sqrt{5}}\right)+C\)

\(\int\frac{7}{4/5}\,\tan^{-1}\frac{4x}{\sqrt{5}}\,+C\)

We have used the more general result:

\(\int\frac{1}{\sqrt{a^{2}-\left(x+b\right)^{2}}}\,\mathrm{d}x=\sin^{-1}\left( \frac{x+b}{a}\right)+C\)\[\begin{array}{l}\mbox{(d)}\ \int\frac{1}{7-6x-x^{2}}\,\mbox{d}x\\ =\int\frac{1}{16-\left(x+3\right)^{2}}\ \mbox{d}x\ \ =\int\frac{1}{4^{2}-\left(x+3 \right)^{2}}\ \mbox{d}x\\ =\frac{1}{2\left(4\right)}\ln\left|\frac{4+\left(x+3\right)}{4-\left(x+3 \right)}\right|+C\\ =\frac{1}{8}\ln\left|\frac{7+x}{1-x}\right|+C\end{array}\quad\begin{array}{l} \mbox{We have used the more general result:}\\ \int\frac{1}{a^{2}-\left(x+b\right)^{2}}\ \mbox{d}x=\frac{1}{2a}\ln\left|\frac{a+ \left(x+b\right)}{a-\left(x+b\right)}\right|+C\\ \mbox{We have used the more general result:}\\ =\left\{\frac{1}{2\left(x^{2}+2x-\frac{5}{2}\right)}\,\mbox{d}x \\ =\frac{1}{2}\left\{\frac{1}{\left(x+1\right)^{2}-\frac{7}{2}}\,\mbox{d}x \\ =\frac{1}{2}\left[\frac{1}{\left(x+1\right)^{2}-\frac{7}{2}}\,\mbox{d}x \\ =\frac{1}{2}\left[\frac{1}{\left(x+1\right)^{2}-\left(\sqrt{\frac{7}{2}} \right)^{2}}\,\mbox{d}x\\ =\frac{1}{2\sqrt{2}}\left(\frac{1}{2\sqrt{2}}\right)\ln\left|\frac{\left(x+1 \right)-\sqrt{\frac{7}{2}}}{\left(x+1\right)+\sqrt{\frac{7}{2}}}\right|+C\\ =\frac{1}{2\sqrt{14}}\ln\left|\frac{\sqrt{2}x+\sqrt{2}-\sqrt{7}}{\sqrt{2}x+ \sqrt{2}+\sqrt{7}}\right|+C\end{array}\right.\]

**Self-Review 11.4**

Find the following integrals:

\[\begin{array}{l}\mbox{(a)}\1.8 Integration of Rational Function of the form \(\int\frac{\mathrm{f}\left(x\right)}{\mathrm{g}\left(x\right)}\mathrm{d}x\), where \(\mathrm{f}(x)\) is a polynomial and \(\mathrm{g}(x)=ax^{2}+bx+c\)

We consider two cases where

1. \(\mathrm{f}(x)\) is a constant and
2 \(\mathrm{f}(x)\) is linear, ie \(\mathrm{f}(x)=dx+e\)

**Note:**: If the fraction \(\frac{\mathrm{f}\left(x\right)}{\mathrm{g}\left(x\right)}\) is improper, you need to do a long division. The integrand would then be reduced to either case 1 or 2.

#### 11.1.8 f(x) is a constant

In this case, we perform the following steps:

1. Complete the square for \(\mathrm{g}(x)\left(=ax^{2}+bx+c\right)\).
2. Refer to MF26 for appropriate formulae in Table 11.5.

Note that we have covered this in Example 11.7b, d and e.

#### 11.1.8.2 f(x) is linear, ie \(\mathrm{f}(x)=dx+e\)

In this case, we perform the following steps:

1. Let \(\mathrm{f}(x)=Ag(x)+B\). Solve for \(A\) and \(B\). Solve for \(A\) and \(B\).
2. Rewrite the integrand \(\int\frac{\mathrm{f}(x)}{\mathrm{g}(x)}\mathrm{d}x=\int\frac{Ag(x)+B}{\mathrm{ g}(x)}\mathrm{d}x=\int\frac{Ag(x)}{\mathrm{g}(x)}\mathrm{d}x+\int\frac{B}{ \mathrm{g}(x)}\mathrm{d}x\) and integrate.

**Example 11.8**:

Find the following integrals:

1. \(\int\frac{x+1}{x^{2}+4\,x+6}\,\mathrm{d}x\) (b) \(\int\frac{9x+7}{9x^{2}+6x+4}\,\mathrm{d}x\)

**Solution:**

\begin{tabular}{|l|l|} \hline (a) & Note that the fraction is proper. \\ \(\mathrm{d}x\) & \(\mathrm{g}\left(x\right)=x^{2}+4x+6\) \\ \(\mathrm{d}x\) & \(\mathrm{g}^{{}^{\prime}}\left(x\right)=2x+4\) \\ \(\mathrm{d}x\) & \(\mathrm{g}^{{}^{\prime}}\left(x\right)=2x+4\) \\ \(\mathrm{d}x\) & \(\mathrm{d}x\) \\ \(\mathrm{d}x\) & \(\mathrm{d}x\) \\ \(\mathrm{d}x\) & \(\mathrm{d}x\) \\ \(\mathrm{d}x\) & \(\mathrm{d}x\) \\ \(\mathrm{d}x\) & \(\mathrm{d}x\) \\ \(\mathrm{d}x\) & \(\mathrm{d}x\) \\ \(\mathrm{d}x\) & \(\mathrm{d}x\) \\ \hline \end{\[\begin{split}&=\frac{1}{2}\ln\left(x^{2}+4x+6\right)-\int\frac{1}{ \left(x+2\right)^{2}+2}\,\,\mathrm{d}x\\ &=\frac{1}{2}\ln(x^{2}+4x+6)-\int\frac{1}{\left(x+2\right)^{2}+ \left(\frac{1}{3}\right)^{2}}\,\,\mathrm{d}x\\ &=\frac{1}{2}\ln(9x^{2}+6x+4)+\frac{4}{9}\Bigg{[}\frac{1}{\sqrt{ 3}}\tan^{-1}\Bigg{[}\frac{x+1}{\sqrt{3}}\Bigg{]}\Bigg{]}+C\\ &=\frac{1}{2}\ln\left(9x^{2}+6x+4\right)+\frac{4}{3\sqrt{3}}\tan^ {-1}\Bigg{[}\sqrt{3}\left(x+\frac{1}{3}\right)\Bigg{]}+C\end{split}\]

**Self-Review 11.5**

Show that \(\int\frac{x}{x^{2}+x-1}\,\mathrm{d}x=\frac{1}{2}\ln\left|x^{2}+x-1\right|-\frac {1}{2\sqrt{5}}\ln\left|\frac{2x+1-\sqrt{5}}{2x+1+\sqrt{5}}\right|+C\,.\)1.9 Integration of Rational Function of the form \(\int\frac{\mathsf{f}\left(x\right)}{\sqrt{\mathsf{g}\left(x\right)}}\,\mathrm{d}x\,,\) where \(\mathsf{f}(x)\) is a polynomial and \(\mathsf{g}(x)=ax^{2}+bx+c\)

#### 11.1.9 \(\mathsf{f}(x)\) is a constant (See Example 11.7a and c)

In this case, we perform the following steps:

1. Complete the square for \(g(x)\!\left(=ax^{2}+bx+c\right)\).
2. Refer to MF26 for appropriate formulae in Table 11.5.

#### 11.1.9 \(\mathsf{f}(x)\) is linear, \(\mathsf{ie}\left(x\right)=dx+e\)

In this case, we perform the following steps:

1. Let \(\mathsf{f}(x)=Ag(x)+B\). Solve for \(A\) and \(B\).
2. Rewrite the integrand \(\int\frac{\mathsf{f}(x)}{\sqrt{\mathsf{g}(x)}}\,\mathrm{d}x=\int \frac{Ag(x)+B}{\sqrt{\mathsf{g}(x)}}\,\mathrm{d}x=\int\frac{Ag(x)}{\sqrt{ \mathsf{g}(x)}}\,\mathrm{d}x+\int\frac{B}{\sqrt{\mathsf{g}(x)}}\,\mathrm{d}x\) \[=A\int\mathsf{g}^{\prime}\left(x\right)\!\left[g\left(x\right) \right]^{\frac{1}{2}}\,\mathrm{d}x+B\!\int\!\frac{1}{\sqrt{\mathsf{g}\left(x \right)}}\,\mathrm{d}x\]

[MISSING_PAGE_EMPTY:15]

**Example 11.10**:

Find \(\int\frac{1}{x^{2}+x-2}\,\mathrm{d}x\).

**Solution:**

\(\frac{1}{x^{2}+x-2}=\frac{1}{\big{(}x-1\big{)}\big{(}x+2\big{)}}=\frac{A}{x-1}+ \frac{B}{x+2}\)

By multiplying the entire equation by \(\big{(}x-1\big{)}\big{(}x+2\big{)}\),

\(1=A\big{(}x+2\big{)}+B\big{(}x-1\big{)}\)

Sub \(x=-2\), \(B=-\frac{1}{3}\)

Sub \(x=1\), \(A=\frac{1}{3}\)

\(\int\frac{1}{x^{2}+x-2}\,\mathrm{d}x=\int\frac{V_{\lambda}}{x-1}+ \frac{-V_{\lambda}}{x+2}\,\mathrm{d}x=\frac{1}{3}\ln\big{|}x-1\big{|}-\frac{1} {3}\ln\big{|}x+2\big{|}+C\)

**Example 11.11**:

Find \(\int\frac{2x+9}{x^{3}+9x}\,\mathrm{d}x\).

**Solution:**

\(\frac{2x+9}{x^{3}+9x}=\frac{2x+9}{x\big{(}x^{2}+9\big{)}}=\frac{Ax+B}{x^{2}+9 }+\frac{C}{x}\)

\(2x+9=\big{(}Ax+B\big{)}x+C\big{(}x^{2}+9\big{)}\)

Sub \(x=0\), \(9=9C\Rightarrow C=1\)

Comparing coefficient of \(x^{2}\) : \(0=A+C\)\(\Rightarrow A=-1\)

Comparing coefficient of \(x\) : \(2=B\)

\(\int\!\!\!\!\!\!\!\!\!\!\!\!

[MISSING_PAGE_EMPTY:17]

[MISSING_PAGE_EMPTY:18]

[MISSING_PAGE_FAIL:19]

**Example 11.14**:

Find \(\int\mathrm{e}^{x}\cos x\ \mathrm{d}x\).

**Solution:**

\[\begin{array}{l}u=\cos x\\ \frac{\mathrm{d}u}{\mathrm{d}x}=-\sin x\mathrm{e}^{-x}=\left[\mathrm{e}^{x} \mathrm{d}x=\mathrm{e}^{x}\right.\end{array}\]

[MISSING_PAGE_EMPTY:21]

[MISSING_PAGE_EMPTY:22]

### The Definite Integral

Let \(\mathrm{F}(x)\) be an anti-derivative of \(\mathrm{f}(x)\) with respect to \(x\) i.e. \(\int\mathrm{f}(x)\,\mathrm{d}x=\mathrm{F}(x)+C\). The **definite integral of \(\mathrm{f}(x)\) with respect to \(x\) from \(x=a\) to \(x=b\)**, denoted by the symbol \(\cdot\int_{a}^{b}\mathrm{f}\left(x\right)\,\mathrm{d}x\,\)', is defined to be \(\mathrm{F}(b)-\mathrm{F}(a)\). That is, \(\int_{a}^{b}\mathrm{f}\left(x\right)\,\mathrm{d}x=\mathrm{F}(b)-\mathrm{F}(a)\), where \(\int\,\mathrm{f}\left(x\right)\,\mathrm{d}x=\mathrm{F}(x)+C\). The values '\(a\)' and '\(b\)' are called the lower and upper limit of the definite integral respectively. Note that unlike the indefinite integral, the definite integral has a numerical value. We will give the precise geometrical meaning of the definite integral in chapter 12.

#### Properties of the Definite Integral

1. \(\int_{a}^{a}\mathrm{f}\left(x\right)\mathrm{d}x=0\)
2. \(\int_{a}^{b}\mathrm{f}\left(x\right)\mathrm{d}x=-\int_{b}^{a}\mathrm{f}\left( x\right)\mathrm{d}x\)
3. \(\int_{a}^{b}\left[\mathrm{f}\left(x\right)\pm\mathrm{g}\left(x\right)\right] \mathrm{d}x=\int_{a}^{b}\mathrm{f}\left(x\right)\mathrm{d}x\pm\int_{a}^{b} \mathrm{g}\left(x\right)\mathrm{d}x\)
4. \(\int_{a}^{b}k\mathrm{f}\left(x\right)\mathrm{d}x=k\int_{a}^{b}\mathrm{f}\left( x\right)\mathrm{d}x\) for any real constant \(k\).
5. \(\int_{a}^{b}\mathrm{f}\left(x\right)\mathrm{d}x=\int_{a}^{c}\mathrm{f}\left(x \right)\mathrm{d}x+\int_{c}^{b}\mathrm{f}\left(x\right)\mathrm{d}x\) where \(a\leq c\leq b\)

**Example 11.16**: Evaluate (a) \(\int_{0}^{\bigvee_{3}}\frac{1}{\sqrt{1-9x^{2}}}\,\mathrm{d}x\), (b) \(\int_{1}^{2}x^{2}\ln x\)\(\mathrm{d}x\)

**Solution:**

\[\begin{array}{|l|l|}\hline\mathrm{(a)}\,\int_{0}^{\bigvee_{3}} \frac{1}{\sqrt{1-9x^{2}}}\,\mathrm{d}x&=\int_{0}^{\bigvee_{3}} \frac{1}{\sqrt{9\left(\frac{1}{9}-x^{2}\right)}}\,\mathrm{d}x&\int \frac{1}{\sqrt{a^{2}-x^{2}}}\,\mathrm{d}x=\sin^{-1}\left(\frac{x}{a}\right)+C \\ &=\frac{1}{3}\left(\sum_{0}^{\bigvee_{3}}\frac{1}{\sqrt{\left(\frac{1}{3} \right)^{2}-x^{2}}}\,\mathrm{d}x\right.&\int_{0}^{\bigvee_{3}} \frac{1}{\sqrt{a^{2}-x^{2}}}\,\mathrm{d}x=\sin^{-1}\left(\frac{x}{a}\right)+C \\ &=\frac{1}{3}\left(\sum_{0}^{\bigvee_{3}}\frac{1}{\sqrt{\left(\frac{1}{3} \right)^{2}-x^{2}}}\,\mathrm{d}x\right.&\int_{0}^{\bigvee_{3}}\frac{1}{\sqrt{a ^{2}

[MISSING_PAGE_EMPTY:24]

[MISSING_PAGE_FAIL:25]

[MISSING_PAGE_EMPTY:26]

[MISSING_PAGE_EMPTY:27]

\(\begin{array}{|l|l|}\hline\text{(b) Using integration by parts,}&u=x\text{ and }\frac{\text{d}v}{\text{d}x}=\cos nx\\ \int_{0}^{\pi}x\cos nx\text{ d}x=&\left[\frac{x}{n}\sin nx\right]_{0}^{\pi}-\frac{1}{n}\int_{0}^{\pi}\sin nx \text{ d}x&\text{d}u=1\text{ and }v=\frac{1}{n}\sin nx\\ &=\frac{\pi}{n}\sin nx-\frac{1}{n^{2}}\left[-\cos nx\right]_{0}^{\pi}&\cos 2\pi= \cos 4\pi\\ &=\frac{1}{n^{2}}\left(\cos mx-1\right)&=\cos 6\pi=\ldots=1\\ &\text{ie }\cos\left(\text{even no.}^{\times}\pi\right)=1\\ &=\left[\begin{matrix}0&\text{if }n\text{ is even}\\ -\frac{2}{n^{2}}&\text{if }n\text{ is odd}\\ \end{matrix}&\cos\pi=\cos 3\pi\\ &=\cos 5\pi=\ldots=-1\\ &\text{ie }\cos\left(\text{odd no.}^{\times}\pi\right)=-1\\ \hline\end{array}\right.\end{array}\)

**Example 11.23** (MC N98 / I / 18 modified):

1. Find the positive integer \(a\) if \(\int_{-2}^{\pi}\frac{1}{4+x^{2}}\text{ d}x=\int_{0}^{\pi}\cos^{2}ax\text{ d}x\).
2. By considering \(\frac{\text{d}}{\text{d}x}\left[\frac{1}{3}\sin\left(x^{3}\right)\right]\) or otherwise, show using integration by parts, that \(\int x^{5}\cos\left(x^{3}\right)\text{d}x=\frac{1}{3}x^{3}\sin\left(x^{3} \right)-\int x^{2}\sin\left(x^{3}\right)\text{d}x\).
3. Find \(\int x^{2}\sin\left(x^{3}\right)\text{d}x\) and hence obtain the exact value of \(\int_{0}^{\pi}\!\!\!x^{5}\cos\left(x^{3}\right)\text{d}x\).

**Solution:**

\(\begin{array}{|l|l|}\hline\text{(a)}&\text{LHS}=\int_{-2}^{\pi}\!\!\!\frac{1 }{4+x^{2}}\text{ d}x=\!\!\int_{-2}^{\pi}\!\!\!\frac{1}{2^{2}+x^{2}}\text{ d}x=&\!\!\!\frac{1}{2}\!\!\left[\tan^{-1}\!\!\left(\frac{x}{2}\right) \right]_{-2}^{\pi}\\ &=&\!\!\!\frac{1}{2}\!\!\left[\tan^{-1}\!\!\left(\frac{a}{2}\right) \!-\!\!\tan^{-

[MISSING_PAGE_FAIL:30]

* [7] In dealing with integrals of the type \[\int\frac{\mathrm{f}\left(x\right)}{\sqrt{\mathrm{g}\left(x\right)}}\;\mathrm{d}x\] where \[\mathrm{g}\left(x\right)\] is a quadratic function which cannot be factorised into real factors, it is often necessary to complete the square for \[\mathrm{g}\left(x\right)\] and apply integrals '3a' and '4a' above. \[\int\frac{\mathrm{f}\left(x\right)}{\sqrt{\mathrm{g}\left(x\right)}}\;\mathrm{d}x\] \[=A\int\mathrm{g}^{\prime}\left(x\right)\left[\mathrm{g}\left(x \right)\right]^{\frac{1}{2}}\;\mathrm{d}x+B\int\frac{1}{\sqrt{\mathrm{g}\left( x\right)}}\;\mathrm{d}x\] where \[A\] and \[B\] are real constants. Then use '3a' for the first integral and '4a' for the second integral.
* [8] For rational fraction, if the denominator is factorisable in linear or quadratic factors, we can find its partial fractions before integrating.
* [9] The technique of substitution transforms the original integral in '\(x\)' into a new integral in the new variable '\(u\)'. Remember to change the original limits of the integral to the corresponding limits in the new variable.
* [10] The integration by parts formula states: This technique is usually applied to integrals consisting of a product of two different functions, for example, \[\int x\sin x\;\mathrm{d}x,\int x^{2}\ln x\;\mathrm{d}x\] The choice of '\(u\)' is guided by the 'LIATE' rule.

[MISSING_PAGE_EMPTY:32]

[MISSING_PAGE_EMPTY:33]

[MISSING_PAGE_EMPTY:34]

1.2 Area of the Region Bounded by a Curve \(y=\mathbf{f}(x)\), the Horizontal Lines \(y=c\) and \(y=d\) and the \(y\)-axis

If a region \(R\) is completely to the right of the \(y\)-axis, then the integral \(\int_{c}^{d}x\ \mathrm{d}y>0\).

\[\begin{array}{c}\mbox{Area of the region $R$ }=\int_{c}^{d}x\ \mathrm{d}y\\ \mbox{\raisebox{-1.29pt}{\includegraphics[height=14.226378pt]{./}}}\end{array}\]

If a region \(L\) is completely to the left of the \(y\)-axis, then the integral \(\int_{c}^{d}x\ \mathrm{d}y<0\).

\[\begin{array}{c}\mbox{Area of the region $L$ }=-\int_{c}^{d}x\ \mathrm{d}y\\ \mbox{\raisebox{-1.29pt}{\includegraphics[height=14.226378pt]{./}}}\end{array}\]

_Again the negative sign is necessary since the integral is negative._

**Example 3**: Find the exact area of the region bounded by the graph of \(y=x^{2}\) where \(x\geq 0\), the lines \(y=1\), \(y=4\) and the \(y\)-axis.

\[\begin{array}{c}\mbox{Solution:}\\ \mbox{\raisebox{-1.29pt}{\includegraphics[height=14.226378pt]{./}}}\end{array}\]

\[\begin{array}{c}\mbox{\raisebox{-1.

[MISSING_PAGE_EMPTY:36]

[MISSING_PAGE_EMPTY:37]

**Independent reading**

Let \(\mathbf{S_{1}}\) be the region above \(x\)-axis and \(\mathbf{S_{2}}\) be the region below \(x\)-axis.

Area of region \(\mathbf{S_{1}}\)

\(=\int_{a}^{b}\mathsf{f}(x)\,\mathrm{d}x-\int_{a}^{c}\mathsf{g}(x)\,\mathrm{d}x- \int_{d}^{b}\mathsf{g}(x)\,\mathrm{d}x\)

Area of region \(\mathbf{S_{2}}=-\int_{c}^{d}\mathsf{g}(x)\,\mathrm{d}x\)

Area of region \(\mathbf{S}\)

\(=\)Area of region \(\mathbf{S_{1}}+\text{Area of region }\mathbf{S_{2}}\)

\(=\left[\int_{a}^{b}\mathsf{f}(x)\,\mathrm{d}x-\int_{a}^{c}\mathsf{g}(x)\, \mathrm{d}x-\int_{d}^{b}\mathsf{g}(x)\,\mathrm{d}x\right]+\left(-\int_{c}^{d} \mathsf{g}(x)\,\mathrm{d}x\right)\)

\(=\int_{a}^{b}\mathsf{f}(x)\,\mathrm{d}x-\left(\int_{a}^{c}\mathsf{g}(x)\, \mathrm{d}x+\int_{c}^{d}\mathsf{g}(x)\,\mathrm{d}x+\int_{d}^{b}\mathsf{g}(x) \,\mathrm{d}x\right)\)

\(=\int_{a}^{b}\mathsf{f}(x)\,\mathrm{d}x-\int_{a}^{b}\mathsf{g}(x)\,\mathrm{d}x\)

\(=\int_{a}^{b}\mathsf{f}(x)-\mathsf{g}(x)\,\mathrm{d}x\)

If we wish to obtain the area of the region as shown on the right, then it is not feasible to perform the integration with respect to \(x\).

Instead, we integrate with respect to \(y\)

as follows:

Shaded area \(=\int_{c}^{d}\left[\ln(y)-\mathsf{k}(y)\right]\mathrm{d}y\)

**Note**: Always use 'right' curve - 'left' curve to find the area between two curves

**Example 5**

Find the exact area of the region bounded by the curve \(y=x^{2}-1\) and the straight line \(y=x+1\).

**Solution:**

\(y=x^{2}-1\)

\(y=x+1\)

\(x\)

\(y=x^{2}-1\)

\(y=x+1\)

\(y=x+1\)

\(x\)

\(y=x^{2}-1\)

\(y=x^{2}-1\)

\(y=x+1\)

\(y=x^{2}-1\)
\[=\int_{-1}^{2}x-x^{2}+2\,\mathrm{d}x=\left[\frac{x^{2}}{2}-\frac{x^{3} }{3}+2x\right]_{-1}^{2}\] \[=\left[2-\frac{8}{3}+4\right]-\left[\frac{1}{2}-(-\frac{1}{3})-2 \right]=4\frac{1}{2}\,\,\mathrm{units}^{2}\]

**Alternative Solution**

Consider region \(R\) as two parts. One portion, \(R_{1}\), is above the \(x\)-axis and the other portion, \(R_{2}\), is below the \(x\)-axis.

\[=\left[\frac{x^{2}}{2}+x\right]_{-1}^{2}-\left[\frac{x^{3}}{3}-x \right]_{1}^{2}\] \[=\left[4-(-\frac{1}{2})\right]-\left[\frac{2}{3}-(-\frac{2}{3}) \right]=4\frac{1}{2}-\frac{4}{3}\,\,\mathrm{units}^{2}\]

Area of region \(R_{2}\)

\[=-\int_{-1}^{1}\left(x^{2}-1\right)\mathrm{d}x=-\left[\frac{x^{3} }{3}-x\right]_{-1}^{2} =-\left[\left(\frac{1}{3}-1\right)-\left(-\frac{1}{3}+1\right)\right]\] \[=\frac{4}{3}\,\,\mathrm{units}^{2}\]

Area of the region \(R=\) Area of region \(R_{1}+\) Area of region \(R_{2}\)

\[=\left(4\frac{1}{2}-\frac{4}{3}\right)+\frac{4}{3}=\,\,4\frac{1}{2}\,\, \mathrm{units}^{2}\]

Which is the easier method?

**Example 6**

Find the exact area bounded by the parabola \(y^{2}=4x\) and the line \(y=2x-4\).

**Solution:**

For the points of intersection,

\((2x-4)^{2}=4x\,\,\Rightarrow x^{2}-5x+4=0\)

\(\Rightarrow(x-1)(x-4)=0\,\,\Rightarrow\,x=1\,\,\mathrm{or}\,\,\,x=4\)

When \(x=1,\,\,y=2(1)-4=-2\)

\(x=4,\,\,y=2(4)-4=4\)

\(y^{2}=4x\Rightarrow\,x=\frac{1}{4}y^{2}\,\,\,\mathrm{and}\,\,\,y=2x-4\,\, \Rightarrow\,x=\frac{1}{2}\,(y+4)\).

\(\therefore\) Shaded area 

[MISSING_PAGE_EMPTY:40]

[MISSING_PAGE_EMPTY:41]

[MISSING_PAGE_EMPTY:42]

[MISSING_PAGE_EMPTY:43]

[MISSING_PAGE_EMPTY:44]

**Self Review 5 (MJC Prelim 2007/1/6 modified)**

A curve is defined by the parametric equations

\[x=t^{3},\;\;y=\frac{1}{1+t^{2}}\;,\;-2\leq t\leq 2\;.\]

Show that the region \(R\) bounded by the axes, the curve and the line \(x=a^{3}\), where \(a>0\) is given by \(\int_{0}^{a}\frac{3t^{2}}{1+t^{2}}\,\mathrm{d}t\). Hence find the exact area of region which is bounded by the axes, the curve and the line \(x=1\).

### Area under a Curve as a Limit of a Sum of the Areas of Rectangles

Let \(\mathrm{f}(x)\) be a function defined on a closed interval \([a,\,b]\).

Suppose we would like to approximate the area of the shaded region \(A\) using \(n\) rectangles. To do so, we divide \([a,b]\) into \(n\) equal subintervals such that the width of each interval \(=\frac{b-a}{n}\). We then construct a rectangle on each subinterval as shown where the height of each rectangle is given by the \(y\)-coordinate on the curve corresponding to the **"left"** endpoint of each subinterval. For example, the height of first rectangle is \(\mathrm{f}(a)\).

Summing up the area of all the rectangles will give us an approximation of the area under the curve.

In this case, the total area of the rectangles is an **under-estimate** of the actual area under the curve.

If the "right" endpoints of each subinterval are used to construct the rectangles instead, the total area of the rectangles will be an **over-estimate** of the actual area under the curve.

**Note:**

Whether total area of rectangles is an under-estimate or over-estimate of the actual area under the curve depends on the **shape** of the graph (e.g. increasing & concave downwards as shown in the figure) and the way the heights of the rectangles are constructed (i.e. "left" or "right" endpoints used). Therefore, you should always have a graph to visualize.

**Example 11**

The diagram shows the graph of \(y=\frac{1}{x}\) in the interval [1, 2] with \(n\) rectangles of equal width.

Show that the total area of \(n\) rectangles, \(S=\sum_{r=1}^{n}\frac{1}{n+r}\).

Deduce the exact value of \(\lim_{n\to\infty}\sum_{r=1}^{n}\frac{1}{n+r}\)

[MISSING_PAGE_EMPTY:47]

[MISSING_PAGE_EMPTY:48]

\[A=\lim_{n\to\infty}\sum_{k=1}^{n}\left[\mathfrak{f}(x_{k})\right]\delta x=\int_{a} ^{b}\left[\mathfrak{f}(x)\right]\mathrm{d}x\.\]

**Self-Review 6**

The diagram shows the graph of \(y=1-x^{2}\) in the interval [0,1] with \(n\) rectangles of equal width.

Show that the total area of the \(n\) rectangles is given by

\[S=1-\frac{1}{n^{3}}\sum_{r=1}^{x}r^{2}\.\]

Deduce the exact value of \(\lim_{n\to\infty}(1-\frac{1}{n^{3}}\sum_{r=1}^{x}r^{2})\)

[Ans: 2/3]

### Volume of a Solid of Revolution as a Limit of a Sum of the Volume of Cylindrical Discs

3.1 Volume of a Solid of Revolution Formed by Rotation About the \(x\)-axis Through \(2\pi\) radians

\(y=\mathfrak{f}(x)\)Let \(\mathrm{f}(x)\) be a function defined on \([a,b]\). The region bounded by the curve \(y=\mathrm{f}(x)\), the \(x\)-axis and the lines \(x=a\) and \(x=b\) is rotated about the \(x\)-axis through \(360^{\circ}\) to form a solid of revolution. We wish to find the volume, \(V\), of the solid of revolution.

Divide the interval \([a,b]\) into \(n\) equal subintervals \([x_{0},x_{1}]\), \([x_{1},x_{2}],\ldots,[x_{n-1},x_{n}]\), each of length \(\delta\alpha=\dfrac{b-a}{n}\), where \(a=x_{0}\) and \(b=x_{n}\).

Let \(\delta V_{k}\) denotes the volume of the \(k^{\mathrm{th}}\) circular disc,

with radius \(\mathrm{f}(x_{k})\) and width \(\delta\alpha\).

Then \(\delta V_{k}=\pi[\mathrm{f}(x_{k})]^{2}\delta\alpha\).

There are \(n\) such discs.

Thus, the total volume of the disks is

\[V_{n}=\sum_{k=1}^{n}\pi[\mathrm{f}(x_{k})]^{2}\delta x\;\;\text{which can be regarded as an approximation}\]

to the value of the actual volume \(V\) of the solid.

We see that as the number of discs increases, i.e as \(n\to\infty\), \(V_{n}\to V\).

Therefore

\[V=\lim_{n\to\infty}\sum_{k=1}^{n}\pi[\mathrm{f}(x_{k})]^{2}\delta x=\int_{a}^ {b}\pi[\mathrm{f}(x)]^{2}\,\mathrm{d}x\]

From our discussion above, the volume of the solid generated when the region \(R\) is rotated through \(2\pi\) radians about the \(x\)-axis is given by

Scan the QR code to visualize the volume of solid of revolution about the \(x\)- axis 

[MISSING_PAGE_EMPTY:51]

[MISSING_PAGE_FAIL:52]

[MISSING_PAGE_EMPTY:53]

[MISSING_PAGE_EMPTY:54]

[MISSING_PAGE_EMPTY:55]

[MISSING_PAGE_EMPTY:56]

[MISSING_PAGE_EMPTY:57]

[MISSING_PAGE_EMPTY:58]

\begin{tabular}{|p{142.3pt}|p{142.3pt}|p{142.3pt}|} \hline (iii) When P is at instantaneous rest, then \(v(t)=0\) & \\ \(t^{2}-3t+2=0\) & \(\Rightarrow\) (\(t-1\))(\(t-2\)) = 0 & \(\Rightarrow\) \(t=1\), 2 \\ \hline \end{tabular}

\begin{tabular}{|p{142.3pt}|p{142.3pt}|} \hline (iv) We have \(s(0)=3\) & The velocity function, \(v(t)\), changes sign at \(t=1\) s and \(t=2\) s. \\ \hline \end{tabular}

\begin{tabular}{|p{142.3pt}|p{142.3pt}|} \hline (iv) We have \(s(0)=3\) & The velocity function, \(v(t)\), changes sign at \(t=1\) s and \(t=2\) s. \\ \hline \end{tabular}

\begin{tabular}{|p{142.3pt}|p{142.3pt}|} \hline (iv) We have \(s(0)=3\) & The velocity function, \(v(t)\), changes sign at \(t=1\) s and \(t=2\) s. \\ \hline \end{tabular}

\begin{tabular}{|p{142.3pt}|p{142.3pt}|} \hline (iv) We have \(s(0)=3\) & The velocity function, \(v(t)\), changes sign at \(t=1\) s and \(t=2\) s. \\ \hline \end{tabular}

\begin{tabular}{|p{142.3pt}|p{142.3pt}|} \hline (iv) We have \(s(1)=\frac{1}{3}(1)^{3}-\frac{3}{2}(1)^{2}+2(1)+3=\frac{23}{6}\) & The velocity function, \(v(t)\), changes sign at \(t=2\) s. \\ \hline \end{tabular}

\begin{tabular}{|p{142.3pt}|p{142.3pt}|} \hline (iv) We have \(s(1)=\frac{1}{3}(2)^{3}-\frac{3}{2}(2)^{2}+2(2)+3=\frac{11}{3}\) & The velocity function, \(v(t)\), changes sign at \(t=1\) s and \(t=2\) s. \\ \hline \end{tabular}

\begin{tabular}{|p{142.3pt}|p{142.3pt}|} \hline (iv) We have \(s(0)=3\) & The velocity function, \(v(t)\), changes sign at \(t=1\) s and \(t=2\) s. \\ \hline \end{tabular}

\begin{tabular}{|p{142.3pt}|p{142.3pt}|} \hline (iv) We have \(s(1)=\frac{1}{3}(2)^{3}-\frac{3}{2}(2)^{2}+2(2)+3=\frac{11}{3}\) & The velocity function, \(v(t)\), changes sign at \(t=2\) s. \\ \hline \end{tabular}

\begin{tabular}{|p{142.3pt}|p{142.3pt}|} \hline (iv) We have \(s(0)=3\) & The velocity function, \(v(t)\), changes sign at \(t=1\) s and \(t=2\) s. \\ \hline \end{tabular}

[MISSING_PAGE_POST]

#### 12.3.7 Miscellaneous Examples

**Example 24** [ACJC Prelim 2017/2/2]

A curve \(C\) has parametric equations \(x=\cos t\), \(y=\dfrac{1}{2}\sin 2t\), where \(\dfrac{\pi}{2}\leq t\leq\dfrac{3\pi}{2}\).

**(i)**: Find the equation of the normal to \(C\) at the point \(P\) with parameter \(p\). [2]

The normal to \(C\) at the point when \(t=\dfrac{2\pi}{3}\) cuts the curve again. Find the coordinates of the point of intersection. [2]
**(ii)**: Sketch \(C\), clearly labelling the coordinates of the points where the curve crosses the \(x\)- and \(y\)-axes. [1]
**(iii)**: Find the Cartesian equation of \(C\).

The region bounded by \(C\) is rotated through \(\pi\) radians about the \(x\)-axis. Find the exact volume of the solid formed. [3]

**Solution:**

**(i)**: \(x=\cos t\)\(\Rightarrow\)\(\dfrac{\mathrm{d}x}{\mathrm{d}t}=-\sin t\)

\(y=\dfrac{1}{2}\sin 2t\)\(\Rightarrow\)\(\dfrac{\mathrm{d}y}{\mathrm{d}t}=\dfrac{1}{2}(2\cos 2t)=\cos 2t\)

\(\dfrac{\mathrm{d}y}{\mathrm{d}x}=\dfrac{\dfrac{\mathrm{d}y}{\mathrm{d}t}}{ \dfrac{\mathrm{d}x}{\mathrm{d}t}}=\dfrac{\cos 2t}{-\sin t}\)\(\Rightarrow\)\(\dfrac{\mathrm{d}y}{\mathrm{d}x}\)\(\Rightarrow\)\(\dfrac{\mathrm{d}y}{-\sin p}\)

\(\Rightarrow\)\(\mathrm{gradient\ of\ normal}=\dfrac{\sin p}{\cos 2p}\)

Equation of normal at the point \(\left(\cos p,\dfrac{1}{2}\sin 2p\right)\):

\(y-\dfrac{1}{2}\sin 2p=\dfrac{\sin p}{\cos 2p}\left(x-\cos p\right)\)

\(\Rightarrow\)\(y=\dfrac{1}{2}\sin 2p+\dfrac{\sin p}{\cos 2p}x-\dfrac{\sin p\cos p}{\cos 2p}\)

\(\Rightarrow\)\(y=\dfrac{\sin p}{\cos 2p}x+\dfrac{\sin 2p}{2}-\dfrac{2\sin p\cos p}{2\cos 2p}\)

\(\Rightarrow\)\(y=\dfrac{\sin p}{\cos 2p}x+\dfrac{1}{2}\left(\sin 2p-\tan 2p\right)\)

Substitute \(p=\dfrac{2\pi}{3}\) into the above to find the equation of normal at this point,

\(y=\dfrac{\sin\dfrac{2\pi}{3}}{\cos 2(\dfrac{2\pi}{3})}x+\dfrac{1}{2} \left(\sin 2(\dfrac{2\pi}{3})-\tan 2(\dfrac{2\pi}{3})\right)\)

[MISSING_PAGE_EMPTY:61]

\[\begin{array}{c}\mbox{Volume required}=\int_{-1}^{0}\pi y^{2}\;\mbox{dx}\\ =\pi\int_{-1}^{0}\left(1-x^{2}\right)x^{2}\;\mbox{dx}=\pi\int_{-1}^{0}x^{2}-x^{ 4}\;\mbox{dx}=\pi\left[\frac{x^{3}}{3}-\frac{x^{5}}{5}\right]_{-1}^{0}\\ =\frac{2}{15}\pi\mbox{ units}^{3}\end{array}\]

**Example 25** (N2009/1/4): It is given that

\[\mbox{f}(x)=\begin{cases}7-x^{2}&\mbox{for }0<x\leq 2,\\ 2x-1&\mbox{for }2<x\leq 4,\end{cases}\]

and that \(\mbox{f}(x)=\mbox{f}(x+4)\) for all real values of \(x\).

(i) Evaluate \(\mbox{f}(27)+\mbox{f}(45)\).

(ii) Sketch the graph of \(y=\mbox{f}(x)\) for \(-7\leq x\leq 10\).

(iii) Find \(\int_{-4}^{3}\mbox{f}(x)\;\mbox{dx}\).

**Solution:**

(i) f(27)

=f(23+4)=f(23)=f(19+4)=f(19)=\cdots=\mbox{f}(3)=5\)

(In fact, \(27=4\times 6+3\;\;\Rightarrow\;\;\mbox{f}(27)=\mbox{f}(3)=5\))

Similarly, \(\mbox{f}(45)=\mbox{f}(11\times 4+1)=\mbox{f}(1)=6\)

\(\therefore\) f(27)+f(45)=5+6=11

(ii)

(iii) \(\int_{-4}^{3}\mbox{f}(x)\;\mbox{dx}=\int_{-4}^{-2}\mbox{f}(x)\;\mbox{dx}+\int _{-2}^{0}\mbox{f}(x)\;\mbox{dx}+\int_{0}^{2}\mbox{f}(x