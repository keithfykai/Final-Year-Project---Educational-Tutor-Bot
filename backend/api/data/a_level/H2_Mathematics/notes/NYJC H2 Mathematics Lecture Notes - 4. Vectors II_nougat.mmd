[MISSING_PAGE_EMPTY:1]

[MISSING_PAGE_EMPTY:2]

[MISSING_PAGE_EMPTY:3]

**Example 4.33**:

Find the value(s) of \(\lambda\) such that the distance of the plane \(\mathbf{r^{*}}\begin{pmatrix}1\\ 2\\ 2\end{pmatrix}=\lambda\) from the origin is 4 units.

\begin{tabular}{|l|l|l|} \hline
**Solution:** & **ThinkZone:** \\ Given \(\mathbf{r^{*}}\begin{pmatrix}1\\ 2\\ 2\end{pmatrix}=\lambda\). & Why are there two values of \(\lambda\)? \\  & What is the geometrical \\  & \(\begin{array}{c}\lambda\lambda\\ \hline\end{array}=4\) & \(\lambda=\lambda\)? \\ \hline \end{tabular}

#### 4.5.2 Equation of a Plane in Cartesian Form

Letting \(\mathbf{r}=\begin{pmatrix}x\\ y\\ z\end{pmatrix}\),\(\mathbf{n}=\begin{pmatrix}n_{1}\\ n_{2}\\ n_{3}\end{pmatrix}\) and substituting into the scalar product form \(\mathbf{r^{*}}\mathbf{n}=D\) gives

\[\begin{pmatrix}x\\ y\\ z\end{pmatrix}\begin{pmatrix}n_{1}\\ n_{2}\\ n_{3}\end{pmatrix}=D\Rightarrow n_{1}x+n_{2}y+n_{3}z=D\]

which is the **cartesian equation** of the plane \(\pi\).

For example, the cartesian equation of the plane \(\mathbf{r^{*}}\begin{pmatrix}1\\ -2\\ 1\end{pmatrix}=-6\) is \(x-2y+z=-6\).

#### 4.5.3 Equation of a Plane in Vector (Parametric) Form

Consider the plane \(\pi\) which is parallel to vectors \(\mathbf{u}\) and \(\mathbf{v}\) (\(\mathbf{u}\) not parallel to \(\mathbf{v}\)) and which also contains the point \(A\) whose position vector is \(\mathbf{a}\). (See Fig. 4.35)

Let \(P\) be any general point on \(\pi\) whose position vector is represented by \(\mathbf{r}\). Since \(\mathbf{u}\) and \(\mathbf{v}\) are two _non-parallel coplanar vectors_ parallel to the plane \(\pi\) and \(\overline{AP}\) is also a vector parallel to \(\pi\), we can therefore write

\[\overline{AP}=\lambda\mathbf{u}+\mu\mathbf{v}\]

for some real scalars \(\lambda\) and \(\mu\), called **parameters**.

By the Triangular law of vector addition,

\[\overline{OP}=\overline{OA}+\overline{AP}\Rightarrow\mathbf{r}=\mathbf{a}+ \lambda\mathbf{u}+\mu\mathbf{v}\]

Fig. 4.35

[MISSING_PAGE_FAIL:5]

[MISSING_PAGE_EMPTY:6]

\begin{tabular}{l|l} \multicolumn{2}{l

[MISSING_PAGE_FAIL:8]

#### 4.5.6 Questions Involving a Plane and a Point

##### 4.5.6.1 Determine if a given point lies on a plane

**Example 4.40**:

Given that \(\pi\): \(\mathbf{r}\)-\(\left[\begin{array}{c}-1\\ 2\\ -1\end{array}\right]\)=1. Determine if the following points lie on the plane.

(a)  \(\left(0,1,1\right)\),  (b) \(\left(-1,2,-1\right)\)

**Solution:**

Since \(\left(\begin{array}{c}0\\ 1\\ 1\\ -1\end{array}\right)\)\(\left[\begin{array}{c}-1\\ 2\\ -1\end{array}\right]\)=2-1=1, the point \(\left(0,1,1\right)\) lies on the plane

Since \(\left(\begin{array}{c}-1\\ 2\\ -1\end{array}\right)\)\(\left[\begin{array}{c}-1\\ 2\\ -1\end{array}\right]\)\(\left[\begin{array}{c}-1\\ 2\\ -1\end{array}\right]\)\(\left[\begin{array}{c}-1\\ -1\end{array}\right]\)\(\left[\begin{array}{c}-1\\ 2\\ -1\end{array}\right]\)\(\left[\begin{array}{c}-1\\ -1\end{array}\right]\)\(\left[\begin{array}{c}-1\\ 2\\ -1\end{array}\right]\)\(\left[\begin{array}{c}-1\\ -1\end{array}\right]\)\(\left[\begin{array}{c}-1\\ 2\\ -1\end{array}\right]\)\(\left[\begin{array}{c}-1\\ -1\end{array}\right]\)

##### 4.5.6.2 Find the

* **perpendicular (shortest) distance from a point to a plane**
* **foot of perpendicular from a point to a plane**
* **reflection of a point in a plane**

Given a plane \(\Pi\) : \(\mathbf{r}\)-\(\mathbf{n}\) = \(D\) and a point \(B\) (not on \(\Pi\)) with position vector \(\mathbf{b}\). We wish to compute the perpendicular distance from \(B\) to \(\Pi\). We present two ways to do this.

The first way involved the idea of using projection. Take an arbitrary point \(A\) with position vector \(\mathbf{a}\) on \(\Pi\) and connect \(B\) to \(A\) to form the vector \(\overline{BA}\).

The perpendicular distance from \(B\) to \(\Pi\), \(BF\), is equal to the length of projection of \(\overline{BA}\) onto \(\mathbf{n}\)

\(=\left|\overline{BA}\mathbf{a}\mathbf{n}\right|\)

\(=\left|\mathbf{a}-\mathbf{b}\right)\)\(\left|\mathbf{n}\right|\)

\(=\frac{\left|\mathbf{a}-\mathbf{b}\right)\)\(\left|\mathbf{n}\right|}{\left|\mathbf{n}\right|}\)

_Mathematics is not about numbers, equations, computations or algorithms; it is about understanding._\[=\frac{\left|\mathbf{a\cdot n}-\mathbf{b\cdot n}\right|}{\left| \mathbf{n}\right|}\] \[=\frac{\left|D-\mathbf{b\cdot n}\right|}{\left|\mathbf{n}\right|}\text { since }\mathbf{a\cdot n}=D_{\cdot\cdot}\text{ (Why?)}\]

Thus, the perpendicular distance from \(B\) (with position vector \(\mathbf{b}\)) to \(\Pi\) having equation \(\mathbf{r\cdot n}=D\) is

Another way to view this formula is from the perspective of distance between two planes. The idea is to create a plane \(\Pi^{\star}\) containing the point \(B\) and parallel to \(\Pi\). The required distance is then the difference between the perpendicular distances from the origin \(O\) to the planes \(\Pi\) and \(\Pi^{\star}\).

The equation of \(\Pi^{\star}\) is given by \(\mathbf{r\cdot n}=\mathbf{b\cdot n}=D^{\star}\) (Note that we use the same normal vector \(\mathbf{n}\) as \(\Pi\)).

From our discussion under remark 3, section 4.5.1, we have

\[\text{perpendicular distance from }B\text{ to }\Pi=\left|\frac{D}{\left|\mathbf{n} \right|}-\frac{D^{\star}}{\left|\mathbf{n}\right|}\right|=\frac{\left|D-D^{ \star}\right|}{\left|\mathbf{n}\right|}=\frac{\left|D-\mathbf{b\cdot n} \right|}{\left|\mathbf{n}\right|}\text{ as before.}\]

Therefore, another expression for finding the perpendicular distance from \(B\) to \(\Pi\) having equation \(\mathbf{r\cdot n}=D\) is

\[\text{where }D^{\star}=\mathbf{b\cdot n}\]

To find the position vector of \(F\), the foot of the perpendicular from \(B\) to \(\Pi\), we find the vector equation of the line \(BF\) and then intersect it with the equation of the plane, \(\pi\), to obtain the position vector of \(F\). Equation of line \(BF\): \(\mathbf{r}=\mathbf{b}+\lambda\mathbf{n},\lambda\in\mathbb{R}\)

**Note:** We use \(\mathbf{n}\) as the direction vector for the line \(BF\) since \(BF\) is perpendicular to the plane and therefore parallel to \(\mathbf{n}\).

Substitute \(\mathbf{r}=\mathbf{b}+\lambda\mathbf{n}\) into \(\mathbf{r\cdot n}=D\) gives

\[\left(\mathbf{b}+\lambda\mathbf{n}\right)\mathbf{n}=D\Rightarrow\mathbf{b\cdot n }+\lambda\mathbf{n\cdot n}=D\Rightarrow\lambda=\frac{D-\mathbf{b\cdot n}}{ \left|\mathbf{n}\right|^{2}}\text{ since }\mathbf{n\cdot n}=\left|\mathbf{n}\right|^{2}.\]

Putting \(\lambda\) back into \(\mathbf{r}=\mathbf{b}+\lambda\mathbf{n}\) gives the position vector of \(F\).

[MISSING_PAGE_EMPTY:11]

\[\left(\begin{array}{c}0\\ 2\\ 5\end{array}\right)+\lambda\left(\begin{array}{c}-2\\ 1\\ 1\end{array}\right)\left(\begin{array}{c}-2\\ 1\\ 1\end{array}\right)=4\] \[7+6\lambda=4\Rightarrow\lambda=-\frac{1}{2}\] Hence \[\overline{OF}=\begin{pmatrix}0\\ 2\\ 5\end{pmatrix}+\left(-\frac{1}{2}\right)\begin{pmatrix}-2\\ 1\\ 1\end{pmatrix}=\begin{pmatrix}1\\ \frac{1}{2}\\ \frac{9}{2}\end{pmatrix}\quad\text{from (*)}\] (iii) By Ratio Theorem, \[\overline{OF}=\frac{1}{2}\overline{O}\overline{A}+\overline{O}\overline{A} ^{\prime}\] \[\Rightarrow\quad\overline{O}\overline{A}^{\prime}=2\overline{OF}- \overline{O}\overline{A}=2\begin{pmatrix}1\\ \frac{1}{2}\\ \frac{9}{2}\end{pmatrix}-\begin{pmatrix}0\\ 2\\ 5\end{pmatrix}=\begin{pmatrix}2\\ 1\\ 4\end{pmatrix}.\]

**Remark:** The method of finding foot of perpendicular from a point to a plane is the same as finding the point of intersection between line and a plane. (See **Example 4.43**)

**Self-Review 4.13 (2008/Prelim/MI/U11 modified)**

Two planes \(\pi_{1}\) and \(\pi_{2}\) have equations \(\mathbf{r}^{\left(\begin{array}{c}2\\ -1\\ 2\end{array}\right)}=9\) and \(\mathbf{r}^{\left(\begin{array}{c}4\\ 3\\ -1\end{array}\right)}=8\) respectively. The point \(P\) has coordinates \((1,-1,\,3)\).

(i) Show that \(P\) lies on plane \(\pi_{1}\).

(ii) Find the shortest distance from \(P\) to plane \(\pi_{2}\). [\(10/\sqrt{26}\) ]

#### Problems Involving a Line and a Plane

##### 4.5.7.1 Determine if a line is parallel or perpendicular to a plane

Given a line \(I\) with direction vector \(\mathbf{d}\) and a plane \(\Pi\) with normal vector \(\mathbf{n}\).

(a) The line and the plane are _parallel_ if and only if \(\mathbf{n}\perp\mathbf{d}\), i.e. \(\mathbf{n}\)**-d** = 0 ;

\(\Pi\) Fig. 4.39

_The concept of vectors is one of the most useful ideas in applied mathematics._E.g. The line \(l_{1}\) with equation \(\mathbf{r}=\begin{pmatrix}1\\ 0\\ 2\end{pmatrix}+\lambda\begin{pmatrix}1\\ -1\\ -1\end{pmatrix}\) is parallel to the plane \(\mathbf{r}=\begin{pmatrix}2\\ -1\\ 3\end{pmatrix}=4\) since \(\mathbf{d}\mathbf{\cdot}\mathbf{n}=\begin{pmatrix}1\\ -1\\ -1\end{pmatrix}\begin{pmatrix}2\\ -1\\ 3\end{pmatrix}=0\). (b) The line and the plane are _perpendicular_ if \(\mathbf{n}//\mathbf{d}\), i.e. \(\mathbf{n}=\lambda\mathbf{d}\) for some \(\lambda\in\mathbb{R}\).

Fig. 4.40

E.g The line \(l_{2}\) with equation \(\mathbf{r}=\begin{pmatrix}1\\ 0\\ 2\end{pmatrix}+\lambda\begin{pmatrix}-2\\ 1\\ -3\end{pmatrix}\) is perpendicular to the plane \(\mathbf{r}=\begin{pmatrix}2\\ -1\\ 3\end{pmatrix}=4\) since \(\mathbf{d}=\begin{pmatrix}-2\\ 1\\ -3\end{pmatrix}=-\begin{pmatrix}2\\ -1\\ 3\end{pmatrix}=-\mathbf{n}\), i.e., \(\mathbf{n}//\mathbf{d}\).

#### 4.5.7.2 Determine if a line lies on a plane

**Example 4.42**

Show that the line \(\mathbf{r}=2\mathbf{i}+2\mathbf{j}+\mathbf{k}+\lambda\left(1+3\mathbf{j}+2 \mathbf{k}\right),\ \lambda\in\mathbb{R}\) lies in the plane \(\mathbf{r}(4\mathbf{i}-2\mathbf{j}+\mathbf{k})=5\).

**Solution:**

\[\begin{array}{l}\text{Let \(\mathbf{a}=\begin{pmatrix}2\\ 1\end{pmatrix}\) be the position vector of a point on the line, \(\mathbf{d}=\begin{pmatrix}1\\ 3\\ 2\end{pmatrix}\) the direction vector of the line, and \(\mathbf{n}=\begin{pmatrix}4\\ -2\\ 1\end{pmatrix}\) the normal vector of the plane.

Step 1: First show that the line is parallel to the plane.

\(\mathbf{d}=\begin{pmatrix}1\\ 3\\ 2\end{pmatrix}\begin{pmatrix}4\\ -2\\ 1\end{pmatrix}=4-6+2=0\)

Hence the line is parallel to the plane.

Step 2: Show that there is a point on the line which also lies on the plane.

\(\mathbf{a}\cdot\mathbf{n}=\begin{pmatrix}2\\ 2\\ 1\end{pmatrix}\begin{pmatrix}4\\ -2\\ 1\end{pmatrix}=8+\begin{pmatrix}-4\\ +1=5\end{pmatrix}\)

The point \(\begin{pmatrix}2,2,1\end{pmatrix}\) on the line also lies in the plane.

Steps 1 and 2 together imply that _all_ points on the line lie in the plane. Hence the line lies in the plane.

#### 4.5.7.3 Determine the point of intersection of a line and a plane

If a line is not parallel to a plane, then they will intersect at _a point_. To find the point of intersection, we substitute the equation of line into the equation of the plane and then solve for the parameter.

**Example 4.43**: Find the point of intersection of the line \(l\colon\mathbf{r}=\begin{pmatrix}1\\ 2\\ 3\end{pmatrix}+\lambda\begin{pmatrix}1\\ -1\\ 3\end{pmatrix}\), \(\lambda\in\mathbb{R}\) and the plane \(\Pi\colon\mathbf{r}\cdot\begin{pmatrix}1\\ 2\\ 2\end{pmatrix}=6\).

**Solution:**

Let \(X\) be the point of intersection of the line \(l\) and the plane \(\Pi\).

Since \(X\) lies on \(l\), \(\overrightarrow{OX}=\begin{pmatrix}1\\ 2\\ 3\end{pmatrix}+\lambda\begin{pmatrix}1\\ -1\\ 3\end{pmatrix}\) for some \(\lambda\in\mathbb{R}\).

Since \(X\) lies on \(\Pi\), \(\overrightarrow{OX}=\begin{pmatrix}1\\ 2\\ 2\end{pmatrix}=6\),

Thus \(\begin{pmatrix}1\\ 2\\ 3\end{pmatrix}+\lambda\begin{pmatrix}1\\ -1\\ 3\end{pmatrix}\begin{pmatrix}1\\ 2\\ 2\end{pmatrix}=6\)\(\Rightarrow 11+5\lambda=6\)\(\Rightarrow\lambda=-1\).

\(\begin{array}{|c|}\hline\mbox{Hence}\;\;\;\overline{OX}=\begin{pmatrix}1\\ 2\\ 3\end{pmatrix}+\begin{pmatrix}1\\ -1\\ 3\end{pmatrix}=\begin{pmatrix}0\\ 3\\ 0\end{pmatrix}.\\ \therefore\) the point of intersection is \((0,\,3,\,0)\). \\ \hline\end{array}\)

**Example 4.44**:

Find the position vector of the point of intersection of the line \(\mathbf{r}=\begin{pmatrix}2\\ 2\\ 0\end{pmatrix}+\lambda\begin{pmatrix}1\\ -1\\ 3\end{pmatrix},\;\lambda\in\mathbb{R}\) and the plane

\(\mathbf{r}=\begin{pmatrix}1\\ -1\end{pmatrix}+s\begin{pmatrix}4\\ 3\\ 0\end{pmatrix}+t\begin{pmatrix}1\\ 1\\ 2\end{pmatrix},\;s,t\in\mathbb{R}\).

**Solution:**

Let the point of intersection be \(X\).

Then \(\overline{OX}=\begin{pmatrix}2\\ 2\\ 0\end{pmatrix}+\lambda\begin{pmatrix}1\\ -1\\ 3\end{pmatrix}\) for some \(\lambda\in\mathbb{R}\) - - (*)

and \(\overline{OX}=\begin{pmatrix}1\\ -1\\ -1\end{pmatrix}+s\begin{pmatrix}4\\ 3\\ 0\end{pmatrix}+t\begin{pmatrix}1\\ 1\\ 2\end{pmatrix}\) for some \(s,\;t\in\mathbb{R}\).

Therefore \(\begin{pmatrix}2\\ 2\\ 0\end{pmatrix}+\lambda\begin{pmatrix}1\\ -1\\ 3\end{pmatrix}=\begin{pmatrix}1\\ -1\\ -1\end{pmatrix}+s\begin{pmatrix}4\\ 3\\ 0\end{pmatrix}+t\begin{pmatrix}1\\ 1\\ 2\end{pmatrix}\)

\(\begin{array}{|c|}2+\lambda=1+4s+t&\lambda-4s-t=-1\\ \Rightarrow&2-\lambda=-1+3s+t\;\;\Rightarrow\;-\lambda-3s-t=-3\\ 3\lambda=-1+2t&3\lambda\;\;-2t=-1\\ \end{array}\)

Solving the above system of linear equations, we \(\begin{array}{|c|}\hline\mbox{\bf ShinkZone:}\\ \mbox{\bf get}\;\;\;\lambda=1,\;s=0,\;t=2\\ \mbox{\bf Sub}\;\;\lambda=1\;\mbox{\bf in (*):}\;\overline{OX}=\begin{pmatrix}2\\ 2\\ 0\end{pmatrix}+\begin{pmatrix}1\\ -1\\ 3\end{pmatrix}=\begin{pmatrix}3\\ 1\\ 3\end{pmatrix}\\ \hline\end{array}\)

**Self-Review 4.14**:

Find the point of intersection of the line \(x-2=2y+1=3-z\) and the plane \(x+2y+z=3\,[(1,-1,4)\,]\)

**4.5.7.4**: **Find the acute angle between a line and a plane**Let \(\theta\) be the acute angle between the line and the plane and \(\phi\) be the _acute_ angle between the line \(l\) and the normal \(\mathbf{n}\). Since \(\phi\) is acute, \(\cos\phi>0\). So

\[\begin{array}{c}\left|\mathbf{n}\mathbf{-d}\right|=\left|\mathbf{n}\right| \left|\mathbf{d}\right|\cos\phi\\ \qquad\qquad=\left|\mathbf{n}\right|\left|\mathbf{d}\right|\cos\left(\frac{ \pi}{2}-\theta\right)\\ \qquad\qquad=\left|\mathbf{n}\right|\left|\mathbf{d}\right|\sin\theta\end{array}\]

The acute angle \(\theta\) between a line with direction vector \(\mathbf{d}\) and a plane with normal vector \(\mathbf{n}\) is given by \(\left|\mathbf{n}\mathbf{-d}\right|=\left|\mathbf{n}\right|\left|\mathbf{d} \right|\sin\theta\), i.e.

\[\sin\theta=\frac{\left|\mathbf{n}\mathbf{-d}\right|}{\left|\mathbf{n}\right| \left|\mathbf{d}\right|}\qquad.\]

**Example 4.45**: Find the acute angle between the plane \(\mathbf{r}\mathbf{-}\left(31-5\mathbf{k}\right)=5\) and the line \(\mathbf{r}=2i-12j+11k+\lambda\left(1+j+k\right)\), \(\lambda\in\mathbb{R}\).

**Solution:**

Let \(\theta\) be the acute angle between the plane and the line.

\[\left(\begin{array}{c}3\\ 0\\ -5\end{array}\right)\left(\begin{array}{c}1\\ 1\\ 1\end{array}\right)=\left(\begin{array}{c}3\\ 0\\ -5\end{array}\right)\left(\begin{array}{c}1\\ 1\\ 1\end{array}\right)\sin\theta\Rightarrow\sin\theta=\frac{2}{\sqrt{34}\sqrt{3}} \Rightarrow\theta=11.4^{\circ}\text{ (1 d.p.)}\text{.}\]

Thus, the acute angle between the plane and the line is \(11.4^{\circ}\).

**Self-Review 4.15**: Find the sine of the acute angle between the line and plane whose equations are

\[\frac{x-2}{2}=\frac{y+1}{6}=\frac{z+3}{3},2x-y-2z=4\text{ \ respectively.}\]

**4.5.7.5**: **Find the image of a line reflected in a plane**

**Example 4.46**: The equation of line \(l\) is \(x-2=\frac{3-y}{2}=\frac{z+1}{-3}\).

(i) Find the coordinates of the point of intersection \(N\) of \(l\) and the plane \(\Pi:\mathbf{r}\left(\begin{array}{c}1\\ -1\\ 2\end{array}\right)=15\).

(ii) Find equation of the line of reflection \(l^{\prime}\) of the line \(l\) in the plane \(\Pi\).

**Solution:**

_The concept of vectors is one of the most useful ideas in the 

[MISSING_PAGE_EMPTY:17]

\(\left[\begin{array}{c}\left(\begin{array}{c}2\\ 3\\ -1\end{array}\right)+\mu\left(\begin{array}{c}1\\ 2\end{array}\right)\right]\left(\begin{array}{c}1\\ -1\\ 2\end{array}\right)=15\)

\(-3+6\mu=15\)

\(\mu=3\)

\(\vdots\)

\(\overline{OF}=\left(\begin{array}{c}2\\ 3\\ -1\end{array}\right)+3\left(\begin{array}{c}1\\ -1\\ 2\end{array}\right)=\left(\begin{array}{c}5\\ 0\\ 5\end{array}\right)\)

\(\left(\begin{array}{c}1\\ -2\\ -3\end{array}\right)\)

Let \(A^{\prime}\) be the reflection of \(A\) in the plane \(\Pi\). By Ratio Theorem,

\(\overline{OF}=\dfrac{1}{2}\left(\overline{OA}+\overline{OA^{\prime}}\right).\)

\(\overline{OA^{\prime}}=2\overline{OF}-\overline{OA}\)

\(\overline{OA^{\prime}}=2\left(\begin{array}{c}5\\ 0\\ 5\end{array}\right)-\left(\begin{array}{c}2\\ 3\\ -1\end{array}\right)=\left(\begin{array}{c}8\\ -3\\ 11\end{array}\right)\)

The reflection \(l^{\prime}\) of the line \(l\) in the plane \(\Pi\) passes through points \(A^{\prime}\) and \(N\):

\(\overline{A^{\prime}N}=\overline{ON}-\overline{OA^{\prime}}=\left(\begin{array} []{c}-4\\ 15\\ 17\end{array}\right)-\left(\begin{array}{c}8\\ -3\\ 11\end{array}\right)=\left(\begin{array}{c}-12\\ 18\\ 6\end{array}\right)\)

Therefore equation of \(l^{\prime}\) is \(\mathbf{r}=\left(\begin{array}{c}8\\ -3\\ 11\end{array}\right)+s\left(\begin{array}{c}-12\\ 18\\ 6\end{array}\right)\), \(s\in\mathbb{R}\)

#### 4.5.7.6 Determine the distance between a parallel line and a plane

To find the distance between a parallel line and a plane, the method is similar to finding the distance from a point \(A\) on the line to the plane.

**Example 4.47**:

_The concept of vectors is one of the most useful ideas in applied mathematics._Show that the line \(l\) whose vector equation is \(\mathbf{r}=2\mathbf{i}-2\mathbf{j}+3\mathbf{k}+\lambda\left(\mathbf{i}-\mathbf{j}+ 4\mathbf{k}\right)\) is parallel to the plane \(\Pi\) whose equation is \(\mathbf{r}-\left(\mathbf{i}+5\mathbf{j}+\mathbf{k}\right)=5\) and find the distance between them.

**Solution:**

Let the direction vector of the line be \(\mathbf{d}\) and the normal of the plane be \(\mathbf{n}\).

**Think Zone**

\(\mathbf{n}\cdot\mathbf{d}=\begin{bmatrix}1\\ 5\\ 1\end{bmatrix}\begin{bmatrix}1\\ -1\\ 4\end{bmatrix}=1-5+4=0\).

Since the normal of the plane \(\Pi\) is perpendicular to the direction vector of the line \(l\), line \(l\) is parallel to the plane \(\Pi\).

Let \(B\) be \((2,-2,3)\). Since \(B\) lies on \(l\), the perpendicular distance between \(l\) and \(\Pi\) is the perpendicular distance between the point \(B\) and \(\Pi\). Thus,

**Self-Review 4.16**

Show that the line \(x+1=y=\dfrac{z-3}{2}\) is parallel to the plane \(\mathbf{r}-\left(\mathbf{i}+\mathbf{j}-\mathbf{k}\right)=3\) and find the distance between them.

**4.5.7.7**

**Find the length of projection of a vector onto a plane**

Length of projection of \(\mathbf{b}\) onto plane \(\Pi=\left|\mathbf{b}\times\mathbf{n}\right|\).

**Fig. 4.43**

**The distance is not about numbers, equations, computations or algorithms; it is about understanding.**

[MISSING_PAGE_EMPTY:20]

\[\begin{array}{c}\text{Planes }\Pi_{3}\colon\mathbf{r}\begin{pmatrix}1\\ 2\\ \end{pmatrix}=3\text{ and }\Pi_{4}\colon\mathbf{r}\begin{pmatrix}2\\ -1\\ 0\end{pmatrix}=4\text{ are perpendicular since }\mathbf{n}_{3}\cdot\mathbf{n}_{4}\begin{pmatrix}1\\ 2\\ 2\end{pmatrix}\begin{pmatrix}2\\ -1\\ 0\end{pmatrix}=0\,.\end{array}\]

5.8.2 Determine the distance between two parallel planes and find the reflection of a plane in another parallel plane

Given two parallel planes with equations in scalar product form

\(\Pi_{1}\colon\mathbf{r}\mathbf{\cdot}\mathbf{n}=D_{1}\) and \(\Pi_{2}\colon\mathbf{r}\mathbf{\cdot}\mathbf{n}=D_{2}\).

Then perpendicular distance from \(O\) to \(\Pi_{1}\) and \(\Pi_{2}\)

are \(\dfrac{D_{1}}{|\mathbf{n}|}\) and \(\dfrac{D_{2}}{|\mathbf{n}|}\) respectively.

Fig. 4.46

The distance between \(\Pi_{1}\) and \(\Pi_{2}\) is given by

**Note:** The normal vector \(\mathbf{n}\) must be the _same_ in both equations.

**Example 4.49**: The equations of three parallel planes are as follows:

\(\Pi_{1}\colon\mathbf{r}\begin{pmatrix}4\\ 3\\ 0\end{pmatrix}=10\), \(\Pi_{2}\colon\mathbf{r}\begin{pmatrix}4\\ 3\\ 0\end{pmatrix}=5\text{ and }\Pi_{3}\colon\mathbf{r}\begin{pmatrix}-4\\ -3\\ 0\end{pmatrix}=15\)

Find the perpendicular distance between

(a) \(\Pi_{1}\) and \(\Pi_{2}\), (b) \(\Pi_{1}\) and \(\Pi_{3}\).

**Solution:**

\(\Pi_{1}\colon\mathbf{r}\begin{pmatrix}4\\ 3\\ 0\end{pmatrix}=10\), \(\Pi_{2}\colon\mathbf{r}\begin{pmatrix}4\\ 3\\ 0\end{pmatrix}=5\text{ and }\Pi_{3}\colon\mathbf{r}\begin{pmatrix}-4\\ -3\\ 0\end{pmatrix}=15\)

Re-write \(\Pi_{3}\colon\mathbf{r}\begin{pmatrix}4\\ 3\\ 0\end{pmatrix}=-15\)

(a) Perpendicular distance between \(\Pi_{1}\) and \(\Pi_{2}=\dfrac{\left|D_{1}-D_{2}\right|}{|\mathbf{n}|}=\dfrac{\left|10-5 \right|}{5}=1\)

_Mathematics is not about numbers, equations, computations or algorithms; it is about understanding._

\begin{tabular}{|l|} \hline Alternatively, distance between \(\Pi_{1}\) and \(\Pi_{2}=\left|\frac{D_{1}}{|\mathbf{n}|}-\frac{D_{2}}{|\mathbf{n}|}\right|=\left| \frac{10}{5}-\frac{5}{5}\right|=\left|2-1\right|=1\) \\ (b) Perpendicular distance between \(\Pi_{1}\) and \(\Pi_{3}=\left|\frac{D_{1}-D_{3}}{|\mathbf{n}|}\right|=\frac{\left|10-\left(-1 5\right)\right|}{5}=5\). \\ Alternatively, distance between \(\Pi_{1}\) and \(\Pi_{3}=\left|\frac{D_{1}}{|\mathbf{n}|}-\frac{D_{3}}{|\mathbf{n}|}\right|= \left|\frac{10}{5}-\frac{\left(-15\right)}{5}\right|=\left|2-\left(-3\right) \right|=5\) \\ \hline \end{tabular}

**Example 4.50**

Show that the planes

\(\pi_{1}\): \(\mathbf{r}=6\mathbf{i}-\mathbf{j}+\mathbf{k}+\lambda\left(4\mathbf{i}-\mathbf{ j}+3\mathbf{k}\right)+\mu\left(\mathbf{j}+\mathbf{k}\right)\) and \(\pi_{2}\): \(\mathbf{r}=2\mathbf{i}+\mathbf{k}+s\left(\mathbf{i}-\mathbf{j}\right)+t\left( 3\mathbf{i}+\mathbf{j}+4\mathbf{k}\right)\) are parallel.

Find

(a) the distance between the planes;

(b) an equation of the mirror image of \(\pi_{1}\) in \(\pi_{2}\).

**Solution:**

\(\mathbf{n}_{1}=\left(\begin{array}{c}4\\ -1\\ 3\end{array}\right)\left(\begin{array}{c}0\\ 1\end{array}\right)=\left(\begin{array}{c}-4\\ -4\\ 4\end{array}\right)=-4\left(\begin{array}{c}1\\ 1\\ -1\end{array}\right)\) and \(\mathbf{n}_{2}=\left(\begin{array}{c}1\\ -1\end{array}\right)\left(\begin{array}{c}3\\ 1\end{array}\right)=\left(\begin{array}{c}-4\\ -4\\ 4\end{array}\right)=-4\left(\begin{array}{c}1\\ 1\\ -1\end{array}\right)\) **ThinkZone**

Since the normal vectors to both planes are parallel (in fact equal), the planes are parallel.

Taking the common normal vector to be \(\left(\begin{array}{c}1\\ 1\\ -1\end{array}\right)\)

\(\pi_{1}\): \(\mathbf{r}\)\(\left(\begin{array}{c}1\\ -1\end{array}\right)=\left(\begin{array}{c}6\\ -1\end{array}\right)\left(\begin{array}{c}1\\ 1\\ 1\end{array}\right)=4\)

\(\pi_{2}\): \(\mathbf{r}\)\(\left(\begin{array}{c}1\\ -1\end{array}\right)=\left(\begin{array}{c}2\\ 0\\ 1\end{array}\right)\left(\begin{array}{c}1\\ -1\end{array}\right)=1\)

(a)Distance between \(\pi_{1}\) and \(\pi_{2}\) is \(\left|\frac{D_{1}}{|\mathbf{n}|}-\frac{D_{2}}{|\mathbf{n}|}\right|=\left| \frac{4}{\sqrt{3}}-\frac{1}{\sqrt{3}}\right|=\frac{3}{\sqrt{3}}=\sqrt{3}\) units.

(b) Let \(\pi_{1}^{\prime}\) be the image of \(\pi_{1}\) reflected in \(\pi_{2}\). \(\pi_{1}^{\prime}\): \(\mathbf{r}\)\(\left(\begin{array}{c}1\\ 1\\ -1\end{array}\right)=D_{1}\)'

_The concept of vectors is one of the most useful ideas in applied mathematics._

[MISSING_PAGE_EMPTY:23]

[MISSING_PAGE_EMPTY:24]

Let \(x=x_{1},y=x_{2},z=x_{3}\) and letting \(x_{3}=\lambda\), we obtain \(x=4+\dfrac{1}{5}\lambda\), \(y=2+\dfrac{4}{5}\lambda\), \(z=\lambda\)

\[\text{Hence }\begin{pmatrix}x\\ y\\ z\end{pmatrix}=\begin{pmatrix}4\\ 2\\ 0\end{pmatrix}+\lambda\begin{pmatrix}\dfrac{1}{5}\\ \frac{4}{5}\\ 1\end{pmatrix}\text{ and writing }\mathbf{r}=\begin{pmatrix}x\\ y\\ z\end{pmatrix},\]

equation of the line of intersection of \(\pi_{1}\) and \(\pi_{2}\) is \(\mathbf{r}=\begin{pmatrix}4\\ 2\\ 0\end{pmatrix}+\lambda\begin{pmatrix}\frac{1}{5}\\ \frac{4}{5}\\ 1\end{pmatrix}\), \(\lambda\in\mathbb{R}\)

**Method 2 (Non-GC): Find a direction vector of the line of intersection and a point on the line**

Let \(\pi_{1}:\mathbf{r}\cdot\mathbf{n}_{1}=D_{1}\) and \(\pi_{2}:\mathbf{r}\cdot\mathbf{n}_{2}=D_{2}\) be two non-parallel planes and \(l:\mathbf{r}=\mathbf{a}+\lambda\mathbf{d}\), \(\lambda\in\mathbb{R}\) the line of intersection of \(\pi_{1}\) and \(\pi_{2}\).

Since \(l\) is parallel to both planes, its direction vector \(\mathbf{d}\) is perpendicular to both \(\mathbf{n}_{1}\) and \(\mathbf{n}_{2}\). Hence the direction vector of the line of intersection of \(\pi_{1}\) and \(\pi_{2}\), \(\mathbf{d}\), is given by

\[\text{\bf d}=\mathbf{n}_{1}\times\mathbf{n}_{2}\]

Finally, a point \(P\) on \(l\) is a common point of \(\pi_{1}\) and \(\pi_{2}\) and this can be found by letting \(\mathbf{r}=\overline{OP}=\begin{pmatrix}x\\ y\\ z\end{pmatrix}\) and substituting into the equations of \(\pi_{1}\) and \(\pi_{2}\) to give _two equations in three unknowns_. This means the system of linear equations has **one degree of freedom** and we can let any of the variables \(x,y\) or \(z\) be any number we like, usually the number \(0\). The other two variables can then be found by solving the two simultaneous equations. The point \(P\) is now determined and the equation of \(l\) can now be written down.

**Example 4.52 (Independent Learning)**

Without the use of GC, find the vector equation of the line of intersection of the pair of planes

\(\pi_{1}\): \(\mathbf{r}=\begin{pmatrix}1\\ 0\\ 2\end{pmatrix}+\lambda\begin{pmatrix}-1\\ 1\\ 0\end{pmatrix}+\mu\begin{pmatrix}1\\ -1\\ -1\end{pmatrix}\)\(\pi_{2}\): \(\mathbf{r}=\begin{pmatrix}2\\ 0\\ 0\end{pmatrix}+s\begin{pmatrix}-1\\ 0\\ 2\end{pmatrix}+\mu\begin{pmatrix}0\\ -3\\ -3\end{pmatrix}\)

**Solution:**

\(\mathbf{n}_{1}=\begin{pmatrix}1\\ -1\\ -1\end{pmatrix}\times\begin{pmatrix}-1\\ 1\\ 0\end{pmatrix}=\begin{pmatrix}1\\ 1\\ 0\end{pmatrix}\), \(\mathbf{n}_{2}=\begin{pmatrix}-1\\ 0\\ 2\end{pmatrix}\times\begin{pmatrix}0\\ 1\\ 1\end{pmatrix}=\begin{pmatrix}-2\\ 1\\ -1\end{pmatrix}\).

\(\mathbf{n}_{1}\times\mathbf{n}_{2}=\begin{pmatrix}1\\ 1\\ 0\end{pmatrix}\times\begin{pmatrix}-2\\ 1\\ -1\end{pmatrix}=\begin{pmatrix}-1\\ 1\\ 3\end{pmatrix}\)

_Mathematics is not about numbers, equations, computations or algorithms; it is about understanding._

[MISSING_PAGE_EMPTY:26]

\begin{tabular}{|c|c|} \hline Set \(z=0\). Then & \begin{tabular}{c} \(2x+y=6\) \\ \(4x-y=2\) \\ \end{tabular} & 
\begin{tabular}{c} Can we set \(y=0\) or \\ \(x=0\) instead? \\ \end{tabular} \\ \hline \end{tabular}

Equation of the line of intersection is

\[\mathbf{r}=\left(\begin{array}{c}\dfrac{4}{3}\\ \dfrac{10}{3}\\ 0\\ \end{array}\right)+\lambda\left(\begin{array}{c}3+\alpha\\ -6+4\alpha\\ -6\end{array}\right),\ \ \lambda\in\mathbb{R}.\]

(b) "Hence" Method:

\(\left(\begin{array}{c}3+\alpha\\ -6+4\alpha\\ -6\end{array}\right)=k\left(\begin{array}{c}2\\ -1\\ -3\end{array}\right)\)

\(\Rightarrow 3+\alpha=2k\)

\(-6+4\alpha=-k\)

\(-6=-3k\Rightarrow k=2\)

\(\therefore\alpha=1\)

"Otherwise" Method:

\(\mathbf{d}\perp\mathbf{n}_{1}\)\(\Rightarrow\)\(\left(\begin{array}{c}2\\ -1\\ -3\end{array}\right)\)\(\left(\begin{array}{c}2\\ 1\\ \alpha\end{array}\right)=0\)\(\Rightarrow\)\(4-1-3\alpha=0\Rightarrow\alpha=1\).

#### 4.5.8.4 Find the angle between two non-parallel planes

Let \(\pi_{1}\) and \(\pi_{2}\) be two planes with normals \(\mathbf{n}_{1}\) and \(\mathbf{n}_{2}\) respectively.

The acute angle \(\theta\) between two planes is given by \(\left|\mathbf{n}_{1}\mathbf{\cdot n}_{2}\right|=\left|\mathbf{n}_{1}\right| \left|\mathbf{n}_{2}\right|\cos\theta\) where \(\mathbf{n}_{1}\) and \(\mathbf{n}_{2}\) are normal vectors of the planes.

**Note:** The modulus is applied to the LHS to ensure that \(\cos\theta>0\) so that \(\theta\) is acute.

The acute angle \(\theta\) between two planes is therefore

\(\cos\theta=\)\(\left|\mathbf{n}_{1}\mathbf{\cdot n}_{2}\right|\)\(\left|\mathbf{n}_{1}\right|\left|\mathbf{n}_{2}\right|\)\(\left|\mathbf{n}_{1}\right|\left|\mathbf{n}_{2}\right|\)

Fig. 4.49

_Mathematics is not about numbers, equations, computations or algorithms; it is about understanding._

**Example 4.54**:

Find the acute angle between the planes \(\mathbf{r(i+j+k)=3}\) and \(\mathbf{r(2i-2j-k)=1}\).

**Solution:**

\(\left|\begin{array}{c}\left(1\right)\\ \left(1\right)\\ -2\\ 1\end{array}\right|=\left(\begin{array}{c}1\\ 1\\ 1\end{array}\right)\left(\begin{array}{c}2\\ -2\\ 1\end{array}\right)\left(\begin{array}{c}\cos\theta\\ -1\end{array}\right)\)

\(\Rightarrow\)\(\cos\theta=\dfrac{1}{3\sqrt{3}}\).

Thus \(\theta=78.9^{\circ}\) (1 d.p.)

**Self-Review 4.17:**

A tetrahedron has vertices at the points \(A(2,-1,0),B(3,0,1),C(1,-1,2),D(-1,3,0)\). Find the cosine of the angle between the faces \(ABC\) and \(ABD\).

\(\left[\dfrac{4}{\sqrt{259}}\right]\)

### Miscellaneous Examples

**Example 4.55 (2014/ACJC Prelim/I/8)**

The planes \(p_{1}\) and \(p_{2}\) have equations \(\mathbf{r(i-1)}\)\(-2\)\(-2\)\(-1\) and \(\mathbf{r(i-7)}\)\(4\)\(4\)\(-1\) respectively.

**(i)** Find the acute angle between \(p_{1}\)and \(p_{2}\).
**(ii)** The point \(A\) (2,\(\alpha\), 3) is equidistant from the planes \(p_{1}\)and \(p_{2}\). Calculate the two possible values of \(\alpha\).

\(\left[5\right]\)
**(iii)** Find the position vector of the foot of perpendicular from \(B\) (0,1, 2) to the plane \(p_{1}\). Hence find the cartesian equation of the plane \(p_{3}\) such that \(p_{3}\) is parallel to \(p_{1}\) and point \(B\) is equidistant from planes \(p_{1}\)and \(p_{3}\).

\(\left[4\right]\)
**(iv)** Find a vector equation of the line of intersection between \(p_{1}\) and \(p_{2}\).

**Solution:**

\(\left[5\right]\)\[\begin{array}{|c|c|}\hline\text{\bf(i)}&\\ &\text{Acute angle between }\ p_{1}\text{ and }\ p_{2}\,\ \theta=\cos^{-1} \left\{\frac{\left(-7\right)\left(4\right)\left(-1\right)}{\sqrt{2^{4}+4^{2} \sqrt{1^{2}+2^{2}+2^{2}}}}\right\}\ =74.97^{\text{o}}=75.0^{\text{o}}\\ \hline\text{\bf(ii)}&\text{Equation of plane containing }\mathcal{A}\text{ and parallel to }\ p_{1}\text{.}\\ &\text{\bf(p_{1}^{\ast}:r\text{\bf(-1)}\left(2\right)=\left(2\right)\left(3 \right)\left(-1\right)}{-2}\right)=4-2\alpha\\ &\text{Equation of plane containing }\mathcal{A}\text{ and parallel to }\ p_{2}\text{.}\\ &\text{\bf(p_{2}^{\ast}:r\text{\bf(-7)}\left(4\right)=\left(2\right)\left( \alpha\right)\left(-7\right)}{-4}\right)=-2+4\alpha\\ &\text{Distance between }\ p_{1}\text{ and }\ p_{1}\ast=\frac{\left|-1-\left(4-2\alpha \right)\right|}{\left(-1\right)}=\frac{1}{\sqrt{1^{2}+2^{2}+2^{2}}}\left|2 \alpha-5\right|=\frac{\left|2\alpha-5\right|}{3}\\ &\text{Distance between }\ p_{2}\text{ and }\ p_{2}\ast=\frac{\left|1-\left(-2+4\alpha \right)\right|}{\left(-7\right)}=\frac{\left|3-4\alpha\right|}{\sqrt{7^{2}+4^ {2}+4^{2}}}=\frac{1}{9}\left|3-4\alpha\right|\\ &\text{Squaring, }\ 3^{2}\left(-5+2\alpha\right)^{2}=\left(3-4\alpha\right)^{2} \Rightarrow\left[3\left(-5+2\alpha\right)+3-4\alpha\right]\left[3\left(-5+2 \alpha\right)-\left(3-4\alpha\right)\right]=0\\ &\text{which simplifies to }\left(\alpha-6\right)\left(5\alpha-9\right)=0\Rightarrow\alpha=6 \text{ or }\ \frac{9}{5}\text{.}\\ \hline\text{\bf(iii)}&\text{Let }M\text{ be the position vector of the foot of perpendicular from }B\text{ to }\ p_{1}\text{.}\\ &\text{Equation of line segment }BM\\ &\text{}r=\begin{pmatrix}0\\ 1\\ 2\end{pmatrix}+\lambda\begin{pmatrix}-1\\ -2\\ 2\end{pmatrix}\\ &\text{When line segment }BM\text{ intersects }p_{1}\text{.}\\ \hline\end{array}\]

[MISSING_PAGE_EMPTY:30]

**Example 4.56** (2017/NYJC JC1 Block Test/I/8)(Application Question):

The diagram (not drawn to scale) shows three vertical flagpoles, \(OF\), \(AG\), \(BH\), with bases \(O\), \(A\), \(B\) respectively on an open and flat field, where \(OA=4\) metres and \(OB=8\) metres. The flagpoles have heights 10 metres, 14 metres and 18 metres respectively. The point \(O\) is taken as the origin, with unit vectors \(\mathbf{i}\) along \(OA\), \(\mathbf{j}\) along \(OB\) and \(\mathbf{k}\) along \(OF\).

The chairman of a neighbourhood committee wants to hold a songbird competition in the open field. He needs to erect a triangular flat shade sail using the flagpoles as supports and with fixing points at \(F\), \(G\) and \(H\).

**(i)** Show that the cartesian equation of this shade sail is \(x+y-z=-10\).

To strengthen the structure, he has to erect another vertical pole \(MN\) where the base \(M\) is at position vector \(2\mathbf{i}+3\mathbf{j}\).

**(ii)** Calculate the height of the pole if \(N\) is just touching the shade sail.

In order to hang some bird cages, he needs to tie a taut rope from the midpoint of \(FH\) to a point \(P\) on \(GH\).

**(iii)** Find the coordinates of \(P\) if the length of the rope used is to be minimum.

**(iv)** To help the residents find the location of the competition easily, the chairman ties a red helium balloon at \(H\) using another rope such that the balloon is vertically above \(H\). Assuming that there is no wind, find the angle between this rope and the shade sail.

**Solution:**

\[\overline{FG}=\begin{pmatrix}0\\ 0\\ 10\end{pmatrix},\overline{HG}=\begin{pmatrix}4\\ 0\\ 14\end{pmatrix},\overline{OH}=\begin{pmatrix}0\\ 8\\ 18\end{pmatrix}\]

\[\overline{FG}=\begin{pmatrix}4\\ 0\\ 4\end{pmatrix},\overline{HG}=\begin{pmatrix}4\\ -8\\ -4\end{pmatrix}\]

[MISSING_PAGE_EMPTY:32]

\(\left[\begin{array}{c}4\\ 0\end{array}\right]\)\(\left[\begin{array}{c}4\\